---
description: Show kind-grouped counts for the current compost seed
allowed-tools: Bash(compost status:*)
---

Run `compost status --json` and summarize for the user: sessions (transcribed / queued / inbox), highlights, codes, themes, frames, and legacy assets per seed. If `--seed` was provided as an argument, pass it through.

Arguments: $ARGUMENTS
