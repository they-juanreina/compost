---
description: Suggest or apply glossary terms / codes across a compost seed
allowed-tools: Bash(compost tag:*), Bash(compost code:*)
---

Use `compost tag` (glossary terms) or `compost code` (codes on highlights) to suggest annotations across the seed. Default to suggestions; only apply with `--apply` when the user confirms. Surface AI suggestions as drafts pending researcher endorsement.

Arguments: $ARGUMENTS
