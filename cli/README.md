# cli

Node/TypeScript CLI engine. Subcommand router, JSON output by default, job-queue runner that drives the harness loops. Every interface (CLI, web, plugin) calls this — there is exactly one mutation path.

See [ROADMAP.md § Tech stack](../ROADMAP.md#tech-stack) and [§ Interfaces](../ROADMAP.md#interfaces--cli--local-nextjs-web).
