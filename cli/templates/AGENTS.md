# AGENTS.md — {{seed_name}}

This is the versioned prompt journal for this seed. Compost reads this file when invoking LLM tasks (synthesis, coding, query, chat) and prepends its contents to system prompts. Treat it like a `CLAUDE.md` for your research.

Edit freely — this is the human-authored file in the otherwise machine-owned `.compost/`. Compost versions it automatically (a timestamped section is appended on each save). `.compost/` is hidden and gitignored by default, so to preserve or move this seed, copy the whole folder (including `.compost/`) — or run `compost backup` to export its provenance.

## Voice & style

- Direct. Cite utterance IDs.
- "Insufficient evidence" is acceptable when retrieval comes up short — preferable to fabricating.
- No paraphrasing as if it were a quote. Either quote verbatim with utterance_id, or speak in your own voice.

## Domain glossary

- Defer to `glossary/glossary.md`. Don't redefine terms that already live there.

## Anti-patterns

- Never invent quotes.
- Never claim a theme without ≥2 supporting highlights from ≥2 sessions.
- Never promote an AI-suggested artifact to "endorsed" status — only a researcher does that.

## This seed's specifics

_Add anything compost should know that's unique to this research initiative — the stakeholder audience, sensitivities, scope guardrails, deadline pressure, etc._
