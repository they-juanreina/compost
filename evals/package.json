{
  "name": "@they-juanreina/compost-evals",
  "version": "0.1.3",
  "description": "Eval surfaces: versioned LLM-as-judge rubric, eval-grader loop, skill golden-set runner.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/they-juanreina/compost.git",
    "directory": "evals"
  },
  "license": "MIT",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "rubric.versions",
    "golden",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "node --import tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "tsx": "^4.19.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
